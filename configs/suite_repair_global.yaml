dataset: "gsm8k"
split: "test"
limit: null 
seed: 42

model_name: "Qwen/Qwen2.5-7B-Instruct" 
max_new_tokens: 256
use_flash_attention: true
use_compile: false

datasets: ["gsm8k"]
dataset_limits:
  gsm8k: 100

methods:
  # The missing Global Anytime SC config (High Budget)
  - name: "global_anytime_sc"
    policy: "cot"
    global_budget_tokens: [100000] # 1000 tokens/example
    init_k: 1
    allocation_policy: ["ucb"]
    max_samples_per_item: 20
    temperature: 0.7

output_dir: "outputs/runs/"
